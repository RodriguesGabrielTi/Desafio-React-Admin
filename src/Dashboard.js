import React from 'react';
import { Card, CardContent, CardHeader } from '@material-ui/core';

export default () => (
    <Card>
        <CardHeader title="Welcome to the SupportAdm" />
        <CardContent>This application was developed to help the administration of the supports in your company</CardContent>
    </Card>
);
